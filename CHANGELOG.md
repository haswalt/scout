# Changelog

Scout's changelog is generated from merged pull requests when a `v*` tag is
pushed. GitHub Releases are the canonical release history.

See the repository's **Releases** page for versioned changes. Pull request labels
control the generated sections through `.github/release.yml`; add
`skip-changelog` to omit internal-only changes.
